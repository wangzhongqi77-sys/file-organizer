---
name: "file-organizer"
description: "将中性来源命名的文件夹（下载目录、网盘导出、搬家目录等）按内容主题分类整理，支持图片/视频音频/PDF 三类，含去重、重命名、空目录清理与归档报告。当用户要求 整理/分类/归类 图片、视频、音频、PDF、素材、下载目录 或提到 白底图、抠图、合同扫描件 时使用。"
---

# 文件按内容分类整理 Skill

把散落在磁盘各处的文件（浏览器下载、AI 生成目录、聊天缓存、合同扫描件等）整理成**按内容主题**分类的目录结构，并做去重、可读化重命名、空目录清理和归档报告。

## 何时使用

- 用户要求整理/分类/归类「图片、视频、音频、PDF、素材、下载目录」
- 来源目录是中性名（`edge下载`、`夸克下载`、`export_xx`、`新建文件夹`、`extracted_images`）
- 需要去重（聊天工具/下载多次产生的重复文件）或把乱码文件名改成可读名

## 通用流程（三领域共用）

```
全盘扫描 → 制定分类规则(先给用户看) → 生成计划(只分析不移动) → 用户确认 → 执行移动+去重 → 回归抽查 → 清空目录 → 归档报告
```

1. **扫描**：按扩展名白名单扫全盘，**排除**系统目录、程序安装目录（图标海量）、聊天缓存（量大且非业务）。只扫业务目录更快更准。
2. **制定分类规则**：浏览文件名提炼主题关键词，**先和用户确认类别命名**（行业不同类别不同）。
3. **生成计划不执行**：导出 plan（CSV/JSON）只分析，人工抽查存疑项后再移动。
4. **执行**：`os.rename` 移动；重名冲突加 `[来源] 前缀` 或 `(N)` 后缀；改/删只读文件先 `os.chmod(p, 0o666)`。
5. **去重**：MD5 判重，目标目录比对 + 批内保留一份。
6. **回归抽查**：对存疑分类逐项视觉/内容核对，修正误判。
7. **清理**：从最深路径开始删空目录（文件数为 0）。
8. **归档**：日志/报告 md 放到目标根目录，追加分类记录。

## 三类分派（详细规则见 REFERENCE.md）

| 领域 | 扩展名 | 核心差异点 |
|---|---|---|
| 图片 | png/jpg/jpeg/gif/webp/bmp | 重命名先行再分类；AI 散图需逐张看；按文件名+目录名双通道扫 |
| 媒体 | mp4/mov/avi/mkv/webm/wmv/flv/m4v/mp3/wav | AI 视频优先判定（jimeng/kling 前缀）；运镜/人物参考是陷阱词 |
| PDF | pdf | MD5 去重 → 规则分类 → OCR 重命名（文本层优先，失败再 OCR） |

详见 [REFERENCE.md](REFERENCE.md) 的三领域分类表、关键词清单与踩坑记录。

## 脚本（scripts/）

确定性操作直接用脚本，比现场生成代码可靠且省 token：

- `scan_plan.py`：扫描 → 规则分类（读外部 JSON 规则）→ 输出计划 CSV，**不改动文件**。用法：`python scan_plan.py <root> --out plan.csv --ext <...> --rules rules.json [--match-dir]`
  - 默认**只按文件名**匹配；加 `--match-dir` 后，文件名未命中才用**所在目录名**兜底，CSV 的 `match_on` 列标注来源是「文件名/目录名」，便于人工复核。
- `execute_plan.py`：读计划执行移动/重命名/去重，输出每类统计 + 报告。用法：`python execute_plan.py plan.csv <dest> --src-root <源目录>`（安全闸：越界路径拒绝移动；不指定则不校验）
- `rename_pdf.py`：PDF 内容识别重命名（pypdf 文本层 → RapidOCR 兜底）。用法：`python rename_pdf.py <dir> --out report.txt`
- `undo.py`：按 execute_plan 的报告反向撤销移动（倒序还原、原位被占跳过不覆盖、`--dry-run` 预览）。用法：`python undo.py report.txt [--dry-run]`

> 脚本生成的计划必须先人工确认再执行；「未分类」兜底项必须提示用户逐张/逐项确认，不静默归类。

## 前置依赖

- Python 3.x（脚本均标准库 + 可选依赖）
- PDF/OCR：`pypdf`、`PyMuPDF(fitz)`、`rapidocr`（`pip install "numpy<2"` 兼容）
- Windows 中文字符：脚本文件存 **UTF-8 带 BOM**，否则文件名乱码误判

## 注意事项

- **先确认类别命名再批量执行**（不同行业/公司类别不同）
- 分类规则和关键词清单建议做成 skill 内置 JSON 资源，跨会话复用
- 保持通用：脚本不内置真实业务数据（公司名/品牌），由用户在会话中提供