# REFERENCE：三领域详细规则

> 本文为 SKILL.md 的详细参考，含各领域分类表、关键词、命名规则与踩坑记录。
> 来源：2026-09-14 实战（媒体 726 / PDF 587 / 图片 2957 个文件）。
> 文中"车A/扫地车A"等为脱敏占位词——使用时换成你行业真实的产品名与型号。

## 一、图片整理（image-organizing）

### 分类体系（三轮车/扫地车行业示例）

| 类别 | 内容 | 主要判定依据 |
|---|---|---|
| 产品图 | 整车/配件/宣传册页 | 车型 + 电机/车把/车轮 |
| 扫地车 | 清扫车/道路清扫 | 清扫/扫地/道路清扫/环卫/扫把/毛刷 |
| 白底图 | 抠图白底产品 | 抠图/白底 + 车型关键词（**白底三轮归三轮车**：展示形式不是品类） |
| 人物图 | 环卫工人/模特/西游人物 | 人物/模特/环卫工/八戒/沙僧 |
| 海报 | 节日/节气/宣传册/logo | 海报/教师节/白露/宣传/主图 |
| 车间素材 | 下料/电泳/焊接/出货 | 车间/激光切割/电泳/烤漆/装车 |
| 视频抽帧 | 宣传视频抽取的帧 | 源目录含 frames/frame |
| MBTI卡通图 | Q版动物人格图 | 海豚/天鹅/ENFJ/旗帜/Q版（多为即梦生成） |

### 关键词优先级（写规则时顺序固定）

1. 人物优先（避免「环卫」同时命中车辆）→ 2. 扫地车 → 3. 三轮车 → 4. 海报 → 5. 车间
- **「清扫宽度」是三轮车参数**（某自卸三轮车英文图），车名关键词必须优先于「清扫」参数关键词，否则 11 张误入扫地车。
- 车名（你行业的产品名/型号，如 车A/车B/X150）优先于功能词（清扫/扫地/装载）。

### 踩坑记录

| 坑 | 解法 |
|---|---|
| 按目录名搜关键词漏图（export_xx/02-Images/新建文件夹/顶层散图） | 文件名扫一轮 + 目录名扫一轮 + 剩余散图逐张看，三轮排查才收敛 |
| AI 生成图（ChatGPT/Gemini/即梦 hash 名）内容混杂 | 只能逐张视觉确认，无法按文件名分类 |
| 重命名兜底用 `产品图_N` | 兜底名无语义，导致后续无法自动分类——**先重命名为可读名再分类** |
| docx/pptx 内嵌图找不到 | 单独提取 `word/media`、`ppt/media` |
| Windows 图标/聊天缓存淹没业务图 | 扫描时排除系统/程序安装/缓存目录 |
| 小缩略图判断车型不可靠 | 拼图缩略图只做初步筛选，**存疑编号必须放大原图确认**（曾误判 14 张蓝三轮为清扫车） |
| 按文件名规则批量迁移后仍会错位 | 迁移后抽样逐张复核（曾发现其他图 1-9.jpg 全为扫地车产品图；"金艺三轮车抠图"实为扫路机 6 张） |
| 历史误命名文件名误导分类 | 名称仅供参考，**以图片内容为准**（"MBTI人格图_白底"实为三轮车 2 张） |
| 纯背景/文字素材图混入产品图 | 抹除主体的背景图（"P掉清扫车"）、纯 LOGO/文字设计图 → 归「背景素材」类，不进产品图 |

### 拼图核验法（批量图片逐张分类的标准做法）

当某目录图片量大（几十到几百张）且无规则可依（AI 散图、同批次产品图），用 **contact_sheet.py** 拼图 + 序号锚定标签，让 AI 对逐张编号做视觉分类：

```
python contact_sheet.py <目录> <TAG> <输出目录> 25 5    # PER=每批张数 COLS=每行张数
# 输出：<TAG>_sheet1.png ... 每张缩略图左下标黄底黑字 #N；order_<TAG>.txt 记录 序号→真实文件名
```

- **不要**在图上标注真实文件名（AI 对中文文件名 OCR 易产生幻觉，报出不存在的文件名）
- 核验结论用「编号 → 目标类别」的 code_map 写 plan.py，再统一生成 JSON 执行移动，moved/fail 双统计
- 一套拼图分多批输出，分批核验，中间穿插复读抽查一致性
- 分类维度先定好再核验（如车型：先看车厢工艺/品牌贴花/车身颜色，肉眼可读的品牌字优先）
- 执行器统一：`os.makedirs(dest, exist_ok=True)` + `os.chmod(src, 0o666)` + 重名加 `(N)` + 日志 UTF-8-sig

### 重命名规则

- 乱码/hash → `车型_场景_序号`（如 `X150三轮车_01`、`车A三轮车_白底`）
- 已有可读名（`1.6米三轮车_卖点图`）保留不重命名

## 二、媒体整理（media-organizing）

### 分类体系（三轮车/扫地车行业示例）

| 类别 | 内容 | 主要判定依据 |
|---|---|---|
| 01-三轮车素材 | 品牌三轮车实拍 | 车A/1.6米/自卸/展厅/减震/大灯特写 |
| 02-扫地车素材 | 扫地车实拍成品 | 扫地车A/B/C/D + 产品型号 |
| 03-AI广告-三轮车 | 即梦/可灵三轮车剧情广告+厂区宣传 | jimeng/kling + 驾驶/载货/品牌介绍/FPV厂区 |
| 04-AI广告-扫地车 | AI清扫车广告 | 清扫/扫地/环卫/毛刷/扫把/垃圾/横幅/致敬 |
| 05-AI广告-西游记 | 西游记创意广告 | 猪八戒/沙和尚/观音/唐僧/西游/师徒/沙漠 |
| 06-人物素材 | 讲解员/西游人物实拍/人格图 | 人格/讲解员/纯人物参考 |
| 07-抖音参考视频 | 同行抖音下载 | 文件名/文件夹含「抖音」 |
| 08-成品视频 | 操作演示/国际站/配音参考 | 产品A/产品B/主图/国际站/配音参考 |
| 09-其他 | 运镜过渡/金毛/动画/模板 | 兜底 |
| 10-音频 | AI配音/抖音参考/配音参考/配套 | 按文件名分组 |

### 核心规则

1. **AI 生成视频优先判定**：`^(jimeng|kling)` 前缀或「即梦生成/AI视频/AI广告/润色/故事性」→ 直接进 AI 分类分支，**先于**实拍关键词判定。
2. **分类顺序（优先级）**：音频 → AI → 文件夹整批 → 文件名关键词 → 兜底「其他」。
3. **重名冲突**：加来源文件夹名前缀 `[来源] 原名`。

### 踩坑记录

| 坑 | 解法 |
|---|---|
| 「运镜/环绕」不是类别 | AI 提示词运镜术语，用它判「其他」会把大量广告误删进其他 |
| 「工地/泥泞」不是扫地车 | 是自卸三轮车常见广告文案；扫地车核心词是 清扫/环卫/毛刷/扫把/条幅/路面无任何垃圾 |
| 「人物参考」≠ 人物素材 | 是 AI 提示词开头，八成是三轮车广告；只有纯人物内容（全局锁定人物/还原面孔/讲解员/实拍）才进人物 |
| 品牌一词多类 | 同一产品名可能横跨两类，按上下文判断（"车A三轮车"=三轮车类；"车A扫灰尘"=扫地车类） |
| 同批次马赛克处理文件无品牌 | 按来源文件夹的主题语境归类 |

## 三、PDF 整理（pdf-organizing）

### 处理流程

```
全盘扫描收集 → MD5 哈希去重 → 规则分类 → 模糊命名识别 → 内容提取 → 重命名
```

### 分类目录（三轮车/扫地车行业示例）

`个人文档 / 产品资料 / 合同 / 商务单据 / 外贸英文 / 扫描件 / 报价单 / 物流单据 / 证件资料 / 采购询价 / 其他`

关键词：合同类（购销合同、协议）、单据类（发票、回单、报价单、PI）、证件类（营业执照、开户）。

### 模糊命名识别（需重命名的文件名模式）

```
32位哈希      ^[0-9a-f]{32}$
纯日期        ^\d{8,}          e.g. 202312251627.pdf
扫描品牌前缀  ^(PDF_?|图片|文字|文档|扫描文稿|扫描|QQ|微信)
无汉字且无英文（表意不明）
```

### 内容提取（重命名的核心）

| 手段 | 工具 | 适用 |
|---|---|---|
| 文本层直接提取 | `pypdf`（PdfReader.extract_text） | 电子生成 PDF |
| 渲染 + OCR | `PyMuPDF(fitz)` 渲染首页 + `RapidOCR` | 扫描件/拍照件 |

先试文本层（快、准），文本 <20 字符或无文本再走 OCR。
OCR 要点：
- 渲染缩放 2~3 倍（`fitz.Matrix(3,3)`）；必要时 PIL 灰度 + 对比度增强
- 中文首选 RapidOCR（onnx 版，ch 模型）
- 只 OCR 前 1~3 页提取标题、甲乙双方、日期、产品名
- **OCR 错别字必须建映射表修正**（如 申方→甲方、数址→数量；把你行业真实错法填进 `rename_pdf.py` 的 `OCR_FIXES`）

### 命名规则（合同类）

```
日期_类型_甲方_乙方_车型.pdf
例：2024-05-15_购销合同_某专用车公司_某贸易公司_单风机道路清扫车.pdf
```

- 日期：合同文本「签订时间/开票日期」优先，缺失回退文件名前 8 位
- 甲方乙方：产品购销合同取「需方」，购销合同取「乙方=购方」；先过滤供方
- 非法字符过滤：`\/:*?"<>|`；文件名限长 60~70 字符
- 重名消解：追加 `(1)(2)` 后缀

### 常见坑

| 坑 | 解法 |
|---|---|
| `UnicodeEncodeError: surrogates` | 提取文本后过滤 `[\ud800-\udfff]` 字符 |
| PDF 损坏（Trailer 报错 / fitz 0 页） | 无法 OCR，保留原名或提示人工/删除 |
| 文件只读 WinError 5 | 删除/改名前置 `os.chmod(p, 0o666)` |
| RapidOCR + numpy 2.x 冲突 | `pip install "numpy<2"` |
| 重命名覆盖风险 | 先输出 plan JSON → 核对 → 再执行；目标已存在即跳过 |
