# Core scripts (scan_plan.py / execute_plan.py / undo.py) use only the Python 3 standard library.
# The deps below are ONLY needed for rename_pdf.py (PDF content recognition & renaming).

pypdf>=3.0
PyMuPDF>=1.23
rapidocr-onnxruntime>=1.3
numpy<2
